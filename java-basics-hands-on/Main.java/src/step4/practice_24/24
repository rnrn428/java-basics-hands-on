public class Todo {
    private boolean done;

    // boolean Getter 작성
    public boolean isDone(){
        return done; 
    }
}

Getter : 반환 타입 있음 (boolean), boolean 타입은 get보다 is로 시작

private boolean active; -> public boolean isActive();
private boolean visible -> public boolean isVisible();
private boolean completed -> public boolean isCompleted();

